"""App configuration for shelves."""
from django.apps import AppConfig


class ShelvesConfig(AppConfig):
    """Configuration for the Shelves app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'shelves'
